#include "main"

/**
 * _abs - computes the absolute value of an integer
 * @r: the integer whose absolute value is required
 * Return: 0
 */

int _abs(int)
{
	int r;
	{
		if (r < 0)
		
			printf("%d\n", r);
		return (1);
	}
}
