#include <stdio.h>

/**
 * Main - Entry point
 *
 * main - Write a program that prints all single digit
 *
 * return: 0
 */

int main()
{
	int num;

	for (num = 48; num <= 57; num++)
		putchar(num);

	putchar('\n');
	return (0);
}
